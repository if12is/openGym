<?xml version="1.0" encoding="utf-8" ?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode|navigation"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data android:name="android.support.FILE_PROVIDER_PATHS" android:resource="@xml/file_paths" />
        </provider>

        <meta-data
            android:name="com.huawei.hms.client.appid"
            android:value="appid=${HUAWEI_APP_ID}" />

        <!-- Health Connect requires the app to be able to explain, from the consent
             screen itself, what it does with health data. Two entry points, because
             the platform changed how it asks between versions:
               · up to Android 13 the permission sheet fires ACTION_SHOW_PERMISSIONS_RATIONALE
               · from Android 14 Health Connect is part of the OS and uses the
                 VIEW_PERMISSION_USAGE alias below
             Without BOTH, the permission picker opens missing its privacy link on
             one version or the other — and on 14+ it can come up empty entirely. -->
        <activity
            android:name=".HealthRationaleActivity"
            android:label="@string/app_name"
            android:exported="true"
            android:excludeFromRecents="true"
            android:theme="@style/AppTheme">
            <intent-filter>
                <action android:name="androidx.health.ACTION_SHOW_PERMISSIONS_RATIONALE" />
            </intent-filter>
        </activity>

        <activity-alias
            android:name="ViewPermissionUsageActivity"
            android:targetActivity=".HealthRationaleActivity"
            android:exported="true"
            android:permission="android.permission.START_VIEW_PERMISSION_USAGE">
            <intent-filter>
                <action android:name="android.intent.action.VIEW_PERMISSION_USAGE" />
                <category android:name="android.intent.category.HEALTH_PERMISSIONS" />
            </intent-filter>
        </activity-alias>
    </application>

    <!-- Lets the app see whether Health Connect is installed at all. Package
         visibility is filtered from Android 11 up, so without this the
         availability check reports "missing" on a phone that has it. -->
    <queries>
        <package android:name="com.google.android.apps.healthdata" />
        <package android:name="com.huawei.health" />
        <package android:name="com.hihonor.health" />
        <package android:name="com.huawei.hwid" />
        <package android:name="com.huawei.hmscore" />
        <!-- The action the androidx client actually builds its picker intent
             with on Android 13 and below. It is ACTION_REQUEST_PERMISSIONS, not
             ACTION_REQUEST_HEALTH_PERMISSIONS — the longer name is the one in
             the manifest docs, and querying for it matches nothing.
             From Android 14 the picker is an ordinary runtime-permission
             request handled inside AndroidX, so no <queries> entry applies. -->
        <intent>
            <action android:name="androidx.health.ACTION_REQUEST_PERMISSIONS" />
        </intent>
        <intent>
            <action android:name="androidx.health.ACTION_SHOW_PERMISSIONS_RATIONALE" />
        </intent>
        <intent>
            <action android:name="androidx.health.ACTION_HEALTH_CONNECT_SETTINGS" />
        </intent>
        <!-- Per-app permission screen. Honor/Huawei often never show the in-app
             picker, so Settings opens this instead. -->
        <intent>
            <action android:name="android.health.connect.action.MANAGE_HEALTH_PERMISSIONS" />
        </intent>
        <intent>
            <action android:name="androidx.health.ACTION_MANAGE_HEALTH_PERMISSIONS" />
        </intent>
        <intent>
            <action android:name="android.health.connect.action.HEALTH_HOME_SETTINGS" />
        </intent>
        <!-- Last-resort route, and the one that always exists. Declared because
             the launcher now resolves every candidate before starting it. -->
        <intent>
            <action android:name="android.settings.APPLICATION_DETAILS_SETTINGS" />
        </intent>
    </queries>

    <!-- Permissions -->

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />
    <!-- Workout-day reminder: lets the local-notifications plugin fire at the exact minute
         on Android 12+. Where it isn't granted, scheduling silently falls back to inexact
         alarms — the reminder still arrives, just not to the minute. -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />

    <!-- Health Connect, read-only. Every permission declared here shows up on one
         consent screen, so the list is kept to what a feature actually renders —
         a long list is the main reason people decline the whole thing.
         Nothing is written back: the app reads what the watch's bridge app
         (Health Sync) has already put in the on-device store. -->
    <uses-permission android:name="android.permission.health.READ_HEART_RATE" />
    <uses-permission android:name="android.permission.health.READ_RESTING_HEART_RATE" />
    <uses-permission android:name="android.permission.health.READ_SLEEP" />
    <uses-permission android:name="android.permission.health.READ_STEPS" />
    <uses-permission android:name="android.permission.health.READ_ACTIVE_CALORIES_BURNED" />
    <uses-permission android:name="android.permission.health.READ_TOTAL_CALORIES_BURNED" />
    <uses-permission android:name="android.permission.health.READ_EXERCISE" />
    <!-- Night-time spot readings, used as a trend rather than a measurement.
         Requested with the rest so the consent screen is asked once, not twice. -->
    <uses-permission android:name="android.permission.health.READ_OXYGEN_SATURATION" />
    <uses-permission android:name="android.permission.health.READ_HEART_RATE_VARIABILITY" />
    <!-- Without this, Health Connect caps every read at roughly the last 30 days
         from the moment access was first granted. It is what makes a year of
         history possible at all — the user still has to grant it separately. -->
    <uses-permission android:name="android.permission.health.READ_HEALTH_DATA_HISTORY" />
</manifest>
